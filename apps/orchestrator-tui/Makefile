.PHONY: test build run

test:
	cargo test -p orchestrator-tui --target-dir target

build:
	cargo build -p orchestrator-tui --target-dir target

run:
	cargo run -p orchestrator-tui --target-dir target
